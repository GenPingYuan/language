import { User } from '../user/user';
import { Media } from '../media/media';

export class Life {
    user: User;
    issueContent: string;
    issueDate: Date;
    issueSource: Media[]
    // cover: 
    // issueMedia:
}
